export class Project{
    projectTitle?: string;
    description?: string;
    techStack?: string[];
    startDate?: Date;
    endDate?: Date;
    liveURL?: string;
    repoURL?: string;
    workingOn?: boolean;
}